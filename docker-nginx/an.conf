upstream AN {
    ip_hash;
    server an-container:5000;
}

server {
    listen 8000;
    location / {
        include proxy_params;
        proxy_pass http://AN/;
    }

    location /static/ {
        alias /static/;
    }
    
    server_name localhost;
}